services:
  wb-min-price-guard:
    build: .
    ports:
      - "3001:3001"
    env_file:
      - .env
    environment:
      HOST: 0.0.0.0
      PORT: 3001
    volumes:
      - wb_repricer_data:/app/data
    restart: unless-stopped

volumes:
  wb_repricer_data:
